import 'package:flutter/material.dart';

class NumberChangeContainer extends StatelessWidget {
  const NumberChangeContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
